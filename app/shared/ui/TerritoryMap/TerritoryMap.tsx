import { TerritoryMapProps } from "./TerritoryMap.type";

const TerritoryMap = ({ items }: TerritoryMapProps) => {
    return (
        <div className="map">
            <h2 className="map__title">карта территории</h2>
            <div className="map__content">
                

                
            </div>
        </div>
    );
}